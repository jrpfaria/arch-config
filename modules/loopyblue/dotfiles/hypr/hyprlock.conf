# Hyprlock configuration with vague theme
general {
    disable_loading_bar = true
    grace = 300
    hide_cursor = true
    no_fade_in = false
}

# Background
background {
    monitor =
    path = ~/.config/swaybg/lock.png
    blur_passes = 1 
    blur_size = 3 
    brightness = 0.5
}

# Input field
input-field {
    monitor = DP-1
    size = 160, 30
    position = 0, -20
    dots_center = true
    fade_on_empty = true
    font_color = rgba(ffffff99)
    inner_color = rgba(5f81a566)
    outer_color = rgba(e8d0bc66)
    outline_thickness = 2
    placeholder_text = 
    shadow_passes = 0 
}

# Current time
label {
    monitor =
    text = cmd[update:1000] echo "$TIME"
    color = rgba(ffffffff)
    shadow_passes = 2
    shadow_size = 0.5
    font_size = 32
    font_family = GohuFont uni14 Nerd Font Propo
    position = 0, 24
    halign = center
    valign = center
}

# User
label {
    monitor =
    text = ♥ Woof!
    color = rgba(7793ADff)
    shadow_passes = 2
    shadow_size = 1
    font_size = 16
    font_family = GohuFont uni14 Nerd Font Mono
    position = 0, 60
    halign = center
    valign = center
}
